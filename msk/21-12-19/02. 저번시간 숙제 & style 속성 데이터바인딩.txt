02. 저번시간 숙제 & style 속성 데이터바인딩

    - :style 속성
        - {}(object) 내에서는 key 값에 "-"를 사용할 수 없어 카멜 표기법으로 작성 
            - background-image (X) 
            - backgroundImage (O)
        - 변수 사용을 원할 경우 ``(백틱) 기호 안에서 ${변수} 형태로 사용하면 편리